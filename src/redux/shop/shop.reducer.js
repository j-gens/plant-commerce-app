import SHOP_DATA from '../../data/shopData.js';


const INITIAL_STATE = {
  shopData: SHOP_DATA,
};

export default shopReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
